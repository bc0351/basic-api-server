name: Run Javascript Tests

on: [push]

env:
  NODE_ENV: production
    - url: 'postgres://lxylucwhxxqihb:cc46e14fd0d75fc367ab12638ef140dd34b40e516d28362144cc6a994cc7e688@ec2-52-206-182-219.compute-1.amazonaws.com:5432/dbem80ln2vujh0'
    - dialect: postgres
    - dialectOptions:
      - ssl:
        - require: true
        - rejectUnauthorized: false

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: npm install, build, and test
      run: |
        npm i
        npm test
      env:
        CI: true
